version: '3.1'

services:
  app:
    image: openjdk:11.0.5-jdk
    container_name: product-comparison-service
    links:
      - mongo
    volumes:
      - ./build/libs:/app
    ports:
      - 8080:8080
  mongo:
    image: mongo
    command: --serviceExecutor adaptive